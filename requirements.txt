# Strict Spec Backend Requirements — no placeholders
# 1. fastapi & uvicorn (ASGI Web server and WebSocket routing)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.1.0

# 2. google-genai (Realtime audio-to-audio streaming loops) — selected per user request
# openai>=1.14.0  # alternative — uncomment if using OpenAI Realtime instead
google-genai>=0.3.0

# 3. openinference-instrumentation-openai & arize-phoenix (spans, latency, tool executions)
openinference-instrumentation-openai>=0.1.8
arize-phoenix>=4.0.0
arize-phoenix-otel>=0.1.0
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp-proto-http>=1.25.0
opentelemetry-exporter-otlp-proto-grpc>=1.25.0  # mandatory for Phoenix Cloud gRPC (fixes 405)

# 4. ragas & datasets (automated evaluation — faithfulness & answer_relevance, multilingual)
ragas>=0.1.9
datasets>=2.18.0

# Document processing — LangChain adapter (full) per user request
langchain>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
# Document parsing — proper PDF/DOCX/XLSX extraction via LangChain loaders
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
docx2txt>=0.8
unstructured>=0.16.0

# Vector DB — pgvector (primary per user request) + embeddings
pgvector>=0.3.5
asyncpg>=0.29.0
# Embeddings — openai + local fallback (sentence-transformers optional)
sentence-transformers>=2.2.0

# Auth & Persistence — Enterprise (SQLAlchemy, async drivers, hashing, JWT)
sqlalchemy>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.19.0
alembic>=1.13.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0
email-validator>=2.1.0
PyJWT>=2.8.0

# Dev / testing (optional)
pytest>=8.0
httpx>=0.27.0
pytest-asyncio>=0.23.0
